#include <stdio.h>
#include <stdlib.h>
#include "arvore.h"

int main(){
    int i = 0, valor = 0;

    Arvore a = criarArvore();
    valor = 
    for(i = 1; i < 10; i++){
        printf("Informe um valor: ");
        scanf("%d", &valor);

        insereAVL(n, valor);
    }
    
    printf("\nArvore informada: ");
        imprimirArvore(a, 2);

    system("pause");
    return 0;
}